// src/constants/productStatus.ts
// Flip these to `true` on launch day — pages stay at the same URL,
// already indexed, and swap their "coming soon" messaging for the live CTAs.
export const WEB_APP_LIVE = false
export const MOBILE_APP_LIVE = false
